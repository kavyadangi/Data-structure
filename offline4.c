//wap func returning an array
#include<stdio.h>
#include<string.h>
printarray(int arr[n]);
int main(){
    int n;
    printf("enter no of values");
    scanf("%d",&n);
    for(int i=0;i<=n;i++){
        scanf("%d"arr[i]);
        
        
 
    }
 
}
